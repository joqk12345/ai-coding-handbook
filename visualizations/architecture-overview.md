# 架构图总览

该页面是面向全书目录的统一架构入口，数据来源于各分部文件夹下全部 `.md` 文件的自动聚合统计。

## 架构与执行可视化

<ArchitectureVisualization />

## 如何使用

1. 先看“全书目录架构分层”，理解每个分部在整体中的内容体量与结构密度。
2. 再看 `content_flow[]` 脉冲流，观察分部在全书路径中的顺序关系。
3. 最后看分部关系流图，把目录层结构映射为可视化拓扑。
